import { POST, PUT, DELETE } from "beddel/server/api/endpointsRoute";

// Next.js route config must be defined locally (can't be re-exported)
export const dynamic = "force-dynamic";

export { POST, PUT, DELETE };
